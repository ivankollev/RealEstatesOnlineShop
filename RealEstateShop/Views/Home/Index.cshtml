@model HomeViewModel

@await Html.PartialAsync("Carousel")

<h2>Prefered real estates of the week</h2>
<h4>Clients preferences - just for you!</h4>

<div class="row">
    @foreach (var realEstate in Model.PrefferedRealEstates)
    {
        @Html.Partial("RealEstatesSummary", realEstate)
    }
</div>